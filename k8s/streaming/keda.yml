apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: streaming-app-scaler
spec:
  scaleTagetReg:
    name: streaming-app
  minReplicaCount: 0
  maxReplicaCount: 100
  polingInterval: 5
  cooldownPeriod: 30
  triggers:
  - type: redis
    metadata:
      address: redis:6379
      listName: cameras:pending
      listLength: "1"
